import Typography from "@mui/material/Typography";
import * as React from "react";

export default function Maps() {
    // TODO: Make maps tab functional
    return (
        <>
            <Typography variant="h1" align="center" style={{marginBottom: 20}}>Maps</Typography>
        </>
    )
}